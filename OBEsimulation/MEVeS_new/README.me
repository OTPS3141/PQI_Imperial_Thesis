
To perform relative imports from different subfolders, we first need to install setup.py

    1. In command line navigate to MeVeS_new folder
    2. Run command python setup.py develop (if you want updated changes to packages)
    2. Run command python setup.py install (if you want just the current version)

    
Paul: write down what you did